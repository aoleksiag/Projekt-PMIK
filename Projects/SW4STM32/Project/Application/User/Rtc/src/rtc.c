#include "rtc.h"


void save_time(void){
/*    HAL_RTC_GetTime(&hrtc, &sTime, RTC_FORMAT_BIN);
    HAL_RTC_GetDate(&hrtc, &sDate, RTC_FORMAT_BIN);
    circ_buffer_put_char(&log_circ_buff, sTime.Hours);
    circ_buffer_put_char(&log_circ_buff, ':');
    circ_buffer_put_char(&log_circ_buff, sTime.Minutes);*/
}

